# Decision Record Pool

Create real decision records here or under `decisions/YYYY/MM/` using
`decisions/templates/decision-template.md`.

Every decision that is old enough to review should eventually receive a sibling
`*-tracking.md` note based on `decisions/templates/tracking-template.md`.
